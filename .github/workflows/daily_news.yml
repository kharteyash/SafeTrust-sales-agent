name: Daily News
on:
  # Triggered by an external scheduler (cron-job.org) that POSTs a
  # repository_dispatch:daily-run at exactly 6:00am ET — fires on time, unlike
  # GitHub's own delayed cron schedule.
  repository_dispatch:
    types: [daily-run]
  # Retry safety net: if the 6am run failed, these firings re-run it every ~30
  # minutes (6:30, 7:00, 7:30, ... ET) until one succeeds. The guard step skips
  # every firing after today's carousels are done (.last_run) and anything
  # outside the 6-11am ET window. Crons are UTC; during EST the first one lands
  # at 5:30am ET and is skipped by the window check. GitHub cron can run a few
  # minutes late — that's fine for a retry.
  schedule:
    - cron: "30 10 * * *"
    - cron: "0 11 * * *"
    - cron: "30 11 * * *"
    - cron: "0 12 * * *"
    - cron: "30 12 * * *"
    - cron: "0 13 * * *"
    - cron: "30 13 * * *"
    - cron: "0 14 * * *"
  workflow_dispatch:
permissions:
  contents: write
# Never let two runs (e.g. a delayed 6am and the 7am cron) overlap.
concurrency:
  group: daily-news
  cancel-in-progress: false
jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Decide whether to run (6-11am Eastern, once per day)
        id: guard
        run: |
          today=$(TZ="America/New_York" date +'%Y-%m-%d')
          echo "today=$today" >> "$GITHUB_OUTPUT"
          event="${{ github.event_name }}"

          # Manual runs always proceed (for testing).
          if [ "$event" = "workflow_dispatch" ]; then
            echo "Manual run — proceeding."
            echo "run=true" >> "$GITHUB_OUTPUT"; exit 0
          fi

          # Run once per day: skip if today's work is already done.
          last=$(cat .last_run 2>/dev/null || echo "")
          if [ "$last" = "$today" ]; then
            echo "Already ran today ($today) — skipping duplicate."
            echo "run=false" >> "$GITHUB_OUTPUT"; exit 0
          fi

          # External cron controls timing exactly — no time-window check needed.
          if [ "$event" = "repository_dispatch" ]; then
            echo "External trigger — proceeding."
            echo "run=true" >> "$GITHUB_OUTPUT"; exit 0
          fi

          # GitHub's own schedule is a delayed fallback — accept the 6-11am ET window.
          hour=$((10#$(TZ="America/New_York" date +%H)))
          if [ "$hour" -lt 6 ] || [ "$hour" -gt 11 ]; then
            echo "Eastern hour is $hour (outside 6-11 morning window) — skipping."
            echo "run=false" >> "$GITHUB_OUTPUT"; exit 0
          fi
          echo "Scheduled fallback in window ($hour ET) — proceeding."
          echo "run=true" >> "$GITHUB_OUTPUT"

      - name: Set up Python
        if: steps.guard.outputs.run == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        if: steps.guard.outputs.run == 'true'
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Scrape the news
        if: steps.guard.outputs.run == 'true'
        run: python scrape.py

      - name: Write carousel copy with Gemini
        if: steps.guard.outputs.run == 'true'
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          CEREBRAS_API_KEY: ${{ secrets.CEREBRAS_API_KEY }}
          XAI_API_KEY: ${{ secrets.XAI_API_KEY }}
          SAMBANOVA_API_KEY: ${{ secrets.SAMBANOVA_API_KEY }}
        run: python write_carousels.py

      - name: Render carousel HTML
        if: steps.guard.outputs.run == 'true'
        run: python generate_carousels.py

      - name: Install Playwright + Chromium
        if: steps.guard.outputs.run == 'true'
        run: |
          pip install playwright
          python -m playwright install --with-deps chromium

      - name: Export slides to PNG
        if: steps.guard.outputs.run == 'true'
        run: python export_slides.py

      - name: Commit the day's output
        if: steps.guard.outputs.run == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          echo "${{ steps.guard.outputs.today }}" > .last_run
          git add .last_run daily_news.json carousel_content.json carousels
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Daily news + carousels ${{ steps.guard.outputs.today }}"
            git push
          fi

      - name: Read carousel titles and captions
        id: titles
        if: steps.guard.outputs.run == 'true'
        run: |
          python - <<'PY' >> "$GITHUB_OUTPUT"
          import json
          data = json.load(open("carousel_content.json"))
          cars = data["carousels"][:3] + data.get("carousels_es", [])[:3]
          print(f"has_es={'true' if len(cars) > 3 else 'false'}")
          for i, c in enumerate(cars, 1):
              title = c.get("title", f"Carousel {i}").replace("\n", " ").strip()
              caption = c.get("caption", "").replace("\r", "").strip()
              print(f"t{i}={title}")
              print(f"c{i}<<CAPTION_EOF")
              print(caption)
              print("CAPTION_EOF")
          PY

      - name: Email carousel 1
        if: steps.guard.outputs.run == 'true'
        uses: dawidd6/action-send-mail@v3
        with:
          # Google Workspace / Gmail SMTP. Change these two lines for another provider.
          server_address: smtp.gmail.com
          server_port: 465
          secure: true
          username: ${{ secrets.MAIL_USERNAME }}
          password: ${{ secrets.MAIL_PASSWORD }}
          subject: "Carousel 1/3 — ${{ steps.titles.outputs.t1 }} (${{ steps.guard.outputs.today }})"
          to: malaez@safetrustmtg.com
          from: SafeTrust Carousels <${{ secrets.MAIL_USERNAME }}>
          body: |
            Carousel 1 of 3 — ${{ steps.titles.outputs.t1 }}
            7 slides attached (1080x1350 PNG).

            Caption:
            ${{ steps.titles.outputs.c1 }}
          attachments: carousels/slides/carousel_1/slide_*.png

      - name: Email carousel 2
        if: steps.guard.outputs.run == 'true'
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          secure: true
          username: ${{ secrets.MAIL_USERNAME }}
          password: ${{ secrets.MAIL_PASSWORD }}
          subject: "Carousel 2/3 — ${{ steps.titles.outputs.t2 }} (${{ steps.guard.outputs.today }})"
          to: malaez@safetrustmtg.com
          from: SafeTrust Carousels <${{ secrets.MAIL_USERNAME }}>
          body: |
            Carousel 2 of 3 — ${{ steps.titles.outputs.t2 }}
            7 slides attached (1080x1350 PNG).

            Caption:
            ${{ steps.titles.outputs.c2 }}
          attachments: carousels/slides/carousel_2/slide_*.png

      - name: Email carousel 3
        if: steps.guard.outputs.run == 'true'
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          secure: true
          username: ${{ secrets.MAIL_USERNAME }}
          password: ${{ secrets.MAIL_PASSWORD }}
          subject: "Carousel 3/3 — ${{ steps.titles.outputs.t3 }} (${{ steps.guard.outputs.today }})"
          to: malaez@safetrustmtg.com
          from: SafeTrust Carousels <${{ secrets.MAIL_USERNAME }}>
          body: |
            Carousel 3 of 3 — ${{ steps.titles.outputs.t3 }}
            7 slides attached (1080x1350 PNG).

            Caption:
            ${{ steps.titles.outputs.c3 }}
          attachments: carousels/slides/carousel_3/slide_*.png

      - name: Email carousel 4 (Spanish 1)
        if: steps.guard.outputs.run == 'true' && steps.titles.outputs.has_es == 'true'
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          secure: true
          username: ${{ secrets.MAIL_USERNAME }}
          password: ${{ secrets.MAIL_PASSWORD }}
          subject: "Carrusel 1/3 (Español) — ${{ steps.titles.outputs.t4 }} (${{ steps.guard.outputs.today }})"
          to: malaez@safetrustmtg.com
          from: SafeTrust Carousels <${{ secrets.MAIL_USERNAME }}>
          body: |
            Carrusel 1 de 3 en español (QuieroUnaCasa.com) — ${{ steps.titles.outputs.t4 }}
            9 slides attached (1080x1350 PNG).

            Caption:
            ${{ steps.titles.outputs.c4 }}
          attachments: carousels/slides/carousel_4/slide_*.png

      - name: Email carousel 5 (Spanish 2)
        if: steps.guard.outputs.run == 'true' && steps.titles.outputs.has_es == 'true'
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          secure: true
          username: ${{ secrets.MAIL_USERNAME }}
          password: ${{ secrets.MAIL_PASSWORD }}
          subject: "Carrusel 2/3 (Español) — ${{ steps.titles.outputs.t5 }} (${{ steps.guard.outputs.today }})"
          to: malaez@safetrustmtg.com
          from: SafeTrust Carousels <${{ secrets.MAIL_USERNAME }}>
          body: |
            Carrusel 2 de 3 en español (QuieroUnaCasa.com) — ${{ steps.titles.outputs.t5 }}
            9 slides attached (1080x1350 PNG).

            Caption:
            ${{ steps.titles.outputs.c5 }}
          attachments: carousels/slides/carousel_5/slide_*.png

      - name: Email carousel 6 (Spanish 3)
        if: steps.guard.outputs.run == 'true' && steps.titles.outputs.has_es == 'true'
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          secure: true
          username: ${{ secrets.MAIL_USERNAME }}
          password: ${{ secrets.MAIL_PASSWORD }}
          subject: "Carrusel 3/3 (Español) — ${{ steps.titles.outputs.t6 }} (${{ steps.guard.outputs.today }})"
          to: malaez@safetrustmtg.com
          from: SafeTrust Carousels <${{ secrets.MAIL_USERNAME }}>
          body: |
            Carrusel 3 de 3 en español (QuieroUnaCasa.com) — ${{ steps.titles.outputs.t6 }}
            9 slides attached (1080x1350 PNG).

            Caption:
            ${{ steps.titles.outputs.c6 }}
          attachments: carousels/slides/carousel_6/slide_*.png
